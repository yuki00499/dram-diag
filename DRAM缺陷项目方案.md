# 基于HPOD-YOLO的DRAM缺陷开放集检测

## 一、文档与项目定位

核心算法全称：

> **HPOD-YOLO：Hierarchical Prototype Open-set Detection YOLO，层次原型开放集检测网络**

研究对象为DRAM后端工艺线SEM缺陷复查图像，目标用户为缺陷复查和失效分析工程师。项目重点解决100多个类别下的长尾分布、极少样本、未知缺陷误判和模型持续扩展问题。

与普通YOLO项目的差异集中在：

- 类别无关的缺陷定位与细分类解耦；
- 缺陷族和细粒度类别的层次化识别；
- 面向极少样本类别的动态多原型学习；
- 面向未见类别的多证据开放集拒识；
- 基于嵌入空间的相似案例检索和原型式增量注册。

## 二、数据审计与路线门槛

取得数据后首先生成数据审计报告，统计图像分辨率、倍率、类别分布、正常样本、标注类型、重复图像、同源多倍率图以及wafer、lot和机台元数据。

按标注形式执行固定路线：

- 有边界框：直接训练HPOD-YOLO检测模型。
- 有分割掩码：由掩码生成检测框，同时保留掩码作为辅助监督。
- 只有图像标签且原图包含较大视野：人工标注首批约200张，通过模型预标注和人工复核补齐其余框。
- 每张图已经是居中的单缺陷ROI：取消无意义的YOLO定位，保留HPOD中的层次原型分类、开放集拒识和检索模块，并在更新版方案中调整标题。
- 无法按wafer或lot划分：使用感知哈希、嵌入聚类和文件元数据排查近重复图，降低训练测试泄漏风险。

不得使用拼接单缺陷图片生成“检测数据”来证明真实定位效果，也不把生成式扩增数据作为最终测试集。

## 三、HPOD-YOLO算法设计

### 1. 类别无关定位

以YOLO11s为可靠基线，保留其骨干网络和多尺度特征融合结构，将检测任务解耦为：

```text
SEM图像
  → YOLO11多尺度特征提取
  → 类别无关缺陷定位分支
  → 缺陷族分类分支
  → 细粒度多原型分支
  → 未知拒识与案例检索
```

定位分支学习“缺陷/背景”和边界框，使样本极少的类别也能借助其他缺陷类别学到通用定位能力。

### 2. 层次化类别建模

依据企业标签构建“缺陷族—细分类”两级体系：

- 一级包含约5–15个具有明确专业含义的缺陷族；
- 二级保留原始100多个专家类别；
- 官方存在分类体系时直接采用；
- 官方没有层次时，由类别说明和视觉聚类形成初稿，再由专业人员审核；
- 不允许仅由大模型根据名称自动确定工艺类别。

样本充足类别参与完整监督训练，2–5张的类别采用少样本原型识别，单样本类别登记到案例库并以检索和人工复核为主，实现对全部类别的分层覆盖。

### 3. 动态多原型学习

从每个检测位置的多尺度特征中提取128维归一化嵌入。每个细分类维护1–3个原型：

- 少于10张：1个原型；
- 10–29张：最多2个原型；
- 30张以上：最多3个原型。

采用监督对比学习压缩同类距离，使用原型分离损失扩大易混类别间隔。层次一致性损失保证细分类结果与所属缺陷族一致。

总损失定义为：

```text
L = L_box + L_DFL + λdL_defect + λhL_hierarchy
    + λpL_prototype + λcL_contrast
```

各损失权重先在验证集确定，之后冻结用于最终测试，不根据测试集调整。

### 4. 开放集拒识

模型将“是否存在缺陷”和“属于哪个类别”分开判断。定位置信度高但类别证据不足的区域进入未知缺陷流程。

未知分数由以下证据融合：

- 最大缺陷族概率；
- 最近细分类原型距离；
- 缺陷族与细分类预测是否一致；
- 轻量多次增强推理的预测分歧。

阈值在独立校准集上确定。输出状态包括：

- 已知缺陷且细分类可信；
- 已知缺陷族但细分类不确定；
- 未知缺陷；
- 低质量图像或低可信结果，建议重新拍摄或人工复核。

### 5. 检索与增量扩展

案例库保存图像区域、归一化嵌入、类别、倍率、来源、人工复核结果和模型版本，使用余弦相似度返回Top-5案例。

人工确认的新类别先通过增加类别原型完成注册；原型注册效果不足时，使用旧类案例回放和知识蒸馏微调最后一个特征层。演示现场不自动更新编码器。

## 四、接口、实验与验收

系统输入支持单张SEM图像和文件夹批量导入。每个检测结果至少包含：

```text
bbox
defect_family
defect_class
confidence
unknown_score
top5_similar_cases
review_required
model_version
```

实验优先按wafer或lot分组划分训练集、校准集和测试集。开放集实验完整留出若干细分类，保证未知类别不参与训练和阈值选择。

对比方法包括：

- 标准YOLO11s；
- YOLO11s加类别平衡；
- 普通层次分类；
- 单原型度量学习；
- 最大Softmax拒识；
- 完整HPOD-YOLO。

主要指标：

- 检测：mAP50、mAP50-95、Recall和漏检率；
- 分类：缺陷族与细分类Macro-F1、Balanced Accuracy和Top-5准确率；
- 开放集：AUROC、AUPR、OSCR和FPR@95TPR；
- 检索：Recall@5、mAP@5和NDCG@5；
- 可信度：ECE和可靠性曲线；
- 工程性能：推理时延、吞吐量和显存占用。

消融实验分别移除类别解耦、层次约束、多原型、监督对比损失和各未知证据。鲁棒性实验覆盖噪声、模糊、对比度、倍率变化和跨机台分布漂移。

暂定验收目标：

- 检测性能不低于标准YOLO11s；
- 细分类Macro-F1相对YOLO11s提高至少5个百分点；
- 未知缺陷AUROC不低于0.85；
- 相似案例Recall@5不低于0.80；
- 8–12GB显存环境可完成训练和离线推理。

绝对精度目标在取得数据并完成首轮基线后更新，不预先填写无法验证的结果。

## 五、交付与时间安排

最终交付以算法研究原型为主：

- HPOD-YOLO训练、推理和评估代码；
- 数据审计与防泄漏脚本；
- 单张及批量诊断界面；
- 缺陷框、层次类别、未知分数和相似案例展示；
- 人工复核与新类别原型注册；
- CSV结果、实验日志和模型版本记录；
- 技术报告、消融结果、答辩PPT和演示视频。

暂不包含没有数据支持的确定性工艺根因推断、机台在线控制、自动在线学习和企业级权限系统。

实施周期按取得数据后七周安排：

1. 第1周：数据审计、标签体系、防泄漏划分和标注方案。
2. 第2周：YOLO11s、分类与检索基线。
3. 第3周：类别无关定位和层次分类。
4. 第4周：动态多原型和监督对比学习。
5. 第5周：开放集拒识、阈值校准和增量注册。
6. 第6周：基线对比、消融、鲁棒性和失败案例分析。
7. 第7周：研究原型、技术报告、视频和答辩材料。

默认项目由个人完成，主要工程工作可由AI辅助，但数据来源、标签含义、论文引用、实验结果和专业结论必须人工核验。取得数据后再根据真实标注形式、类别数量和元数据生成V1.0正式方案。
