experiment: ge15_coverage_comparison
data_root: 晶圆缺陷分类数据集
manifest: artifacts/ge15/split_manifest.json
out_dir: runs/ge15_coverage_comparison
backbone: resnet18
image_size: [480, 320]
normalization: imagenet
loss: ce
epochs: 60
patience: 12
freeze_epochs: 5
batch_size: 8
num_workers: 0
lr: 0.0003
backbone_lr: 0.0001
weight_decay: 0.0001
seeds: [42]
